{
  "buildCommand": "cd backend && pip install -r requirements.txt && cd ../dream-home-design && npm install && npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dream-home-design/dist",
  "env": {
    "PYTHONUNBUFFERED": "1"
  },
  "functions": {
    "backend/app/main.py": {
      "runtime": "python3.13"
    }
  }
}
